namespace R3E05_AlbertoDM
{
    internal class Program
    {
        static void Main(string[] args)
        {
            // CONSTANTES

            // VARIABLES
            // Declaramos las varaibles en numeros flotantes
            float base1;
            float altura;
            float superficie;
            string cadenaAuxiliar;

            // ENTRADA
            // Preguntamos por la base
            Console.Write("Indica cuanto mide la base del rectangulo: ");
            // Mandamos leer la cadena auxiliar, la convertimos a decimal con Convert.ToSingle y le transferimos el valor a base
            cadenaAuxiliar = Console.ReadLine();
            base1 = Convert.ToSingle(cadenaAuxiliar);
            // Hacemos lo mismo con la altura, preguntamos, leemos y convertimos
            Console.Write("Indica la altura del rectangulo: ");

            // PROCESO

            // SALIDA

        }
    }
}
